DefVarsN3();

val = VecN3(x,y,z);
var = VecN3(a,b,c);
i = 0;
// test case without unrolling
loop {
	if (i > 9) {
		break;
	}
	if (unknown) {
		var = var + e2 + e3^einf;
		
		j = 0;

		loop {
			if (j > 4) {
				break;
			}
			var = var + 1 + e1^e2^e3^einf^e0;
			j = j + 1;
		}
	}
	val = val + 1;
	i = i + 1;
}
?val;
?var;
