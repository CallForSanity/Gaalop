language: cpp

compiler:  
    - gcc
    - clang

before_script:  
    - sudo apt-get update -qq
    - sudo apt-get install -y maven
    - mkdir build
    - cd build
    - cmake ..

script: make

after_script:
    - make test
