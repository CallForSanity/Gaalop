DefVarsN3();

multiplyByTen = {
	i = 0;
	x = 0;
	//#pragma count i
	loop {
		if (i > 9) {
			break;
		}
		x = x + _P(1);
		i = i + 1;
	}
	x
}

getX = { ::x }
setX = { ::x = _P(1); }

i = 0;
//#pragma count i
loop {
	if (i > 9) {
		break;
	}
	setX(multiplyByTen(getX()));
	i = i + 1;
}

?x;
