package datapath.graph.display.dot;

import datapath.graph.display.DisplayEdge.Color;
import datapath.graph.display.DisplayEdge.Type;

/**
 *
 * @author jh
 */
public class DisplayEdge extends datapath.graph.display.DisplayEdge<DisplayNode> {

    public DisplayEdge(DisplayNode source, DisplayNode target, Type type,
            Color color) {
        super(source, target, type, color);
    }





}
